name: Deploy site (liftquest + qrsafety) to GitHub Pages

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
        with:
          enablement: true
      # liftquest は従来どおりサイトルートに配置し、qrsafety を /qrsafety/ に追加する
      - name: Assemble site
        run: |
          mkdir -p _site
          cp -r liftquest/. _site/
          cp -r qrsafety _site/qrsafety
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deployment
        uses: actions/deploy-pages@v4
